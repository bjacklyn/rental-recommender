name: Chat-App Backend CI/CD

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-and-deploy:
    name: Test and Deploy
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout the code
    - name: Checkout Code
      uses: actions/checkout@v3

    # Step 2: Set up Python and run tests
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
      # pip install -r backend/requirements.txt-

#    - name: Run Tests
#      working-directory: chat-app/backend
#      run: pytest --maxfail=5 --disable-warnings --verbose

    # Step 3: Set up Docker
#    - name: Set up Docker Buildx
#      uses: docker/setup-buildx-action@v2
#
#    - name: Log in to DockerHub
#      uses: docker/login-action@v2
#      with:
#        username: ${{ secrets.DOCKER_USERNAME }}
#        password: ${{ secrets.DOCKER_PASSWORD }}

    # Step 4: Build the Docker image
    - name: Build and Tag Docker Image
      run: |
        docker build . --tag chat-app:1.0

    # Step 5: Deploy the container locally (Optional for CI, add this step for a local runner)
    - name: Run Docker Container Locally
      run: |
        docker run --rm --init  -it  -p 8000:8000 chat-app:1.0
